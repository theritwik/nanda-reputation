# Upstash Redis REST API credentials (free tier: https://upstash.com)
# Optional: without these, the service falls back to an in-memory store
# that resets on every restart. Required for persistent storage on Vercel.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
